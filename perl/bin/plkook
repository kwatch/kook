#!/usr/bin/env perl

use strict;
use Data::Dumper;
use Kook::Main;

my @argv = @ARGV;
my $main = $argv[0] eq "-X"
         ? Kook::MainApplication->new(\@argv)
         : Kook::MainCommand->new(\@argv);
$main->main();
