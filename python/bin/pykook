#!/usr/bin/env python
# -*- coding: utf-8 -*-

import sys
from kook.main import MainCommand, MainApplication

shebang_p = len(sys.argv) >= 2 and sys.argv[1] in ('-X', )
klass  = shebang_p and MainApplication or MainCommand
status = klass(sys.argv).main()
sys.exit(status)
